{
  "name": "businessos",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "test": "npm run test -w backend && npm run test -w frontend",
    "lint": "eslint .",
    "db:up": "docker compose up -d postgres maildev adminer",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "eslint": "^9.15.0"
  }
}
